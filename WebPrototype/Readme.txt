Note - gradle(build automation tool) is required to build the project
//Required Jar files are not included with this project, gradle will automatically download all required Jar's 

Steps to run project
1.Go to the root folder of project(WebPrototype) and execute below command
gradle clean build run
2. Once project is successfully compiled then start the application in web browser using below link 
http://localhost:9000/